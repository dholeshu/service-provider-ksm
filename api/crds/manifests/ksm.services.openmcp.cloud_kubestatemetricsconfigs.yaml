---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  labels:
    openmcp.cloud/cluster: onboarding
  name: kubestatemetricsconfigs.ksm.services.openmcp.cloud
spec:
  group: ksm.services.openmcp.cloud
  names:
    kind: KubeStateMetricsConfig
    listKind: KubeStateMetricsConfigList
    plural: kubestatemetricsconfigs
    singular: kubestatemetricsconfig
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.configMapName
      name: ConfigMap
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: KubeStateMetricsConfig is the Schema for the kubestatemetricsconfigs
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of KubeStateMetricsConfig
            properties:
              additionalConfigs:
                additionalProperties:
                  type: string
                description: |-
                  AdditionalConfigs allows for additional configuration files to be created
                  Key is the filename, value is the file content
                type: object
              config:
                description: |-
                  Config contains the standard kube-state-metrics configuration
                  This is the content that will be written to config.yaml for non-custom resources
                type: string
              customResourceStateConfig:
                description: |-
                  CustomResourceStateConfig contains the custom resource state metrics configuration
                  This is the content that will be written to custom-resource-state-config.yaml
                type: string
              targetNamespace:
                default: observability
                description: |-
                  TargetNamespace is the namespace where the ConfigMap will be created
                  This should match the namespace where kube-state-metrics is deployed
                  If not specified, defaults to "observability"
                type: string
            type: object
          status:
            description: status defines the observed state of KubeStateMetricsConfig
            properties:
              conditions:
                description: conditions represent the current state of the KubeStateMetricsConfig
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              configMapName:
                description: ConfigMapName is the name of the ConfigMap created for
                  this configuration
                type: string
              configMapNamespace:
                description: ConfigMapNamespace is the namespace of the ConfigMap
                  created for this configuration
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation of this resource
                  that was last reconciled by the controller.
                format: int64
                type: integer
              phase:
                description: Phase is the current phase of the resource.
                type: string
            required:
            - observedGeneration
            - phase
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
